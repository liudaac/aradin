package cn.aradin.spring.salarm.starter.enums;

/**
 * level of alarm. It supports configure levels for each alarm and support-alarms for each handler.
 * @author daliu
 *
 */
public enum SalarmLevel {
	trace,
	info,
	warn,
	error;
}
