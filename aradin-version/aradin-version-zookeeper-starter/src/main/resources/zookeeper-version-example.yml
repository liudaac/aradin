aradin:
  version:
    zookeeper:
      address-id: ${customid}
  zookeeper:
    #enable: true
    #session-timeout: 5000
    #connection-timeout: 5000
    addresses:
      - id: ${customid}
        address: 