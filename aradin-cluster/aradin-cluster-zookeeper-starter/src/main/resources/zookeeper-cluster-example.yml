aradin:
  cluster:
    name: default
    node-name: ${HOST}
    register: true
    prefer-ip-address: true
    zookeeper:
      address-id: ${customid}
  zookeeper:
    enable: true
    session-timeout: 5000
    connection-timeout: 5000
    addresses:
      - id: ${customid}
        address: 